<% content_for(:title) { "Nous contacter | ProfConnect" } %>

<div class="min-h-screen bg-white text-gray-900 pb-20 md:pb-0">
  <!-- Mobile Header (visible only on mobile) -->
  <header class="md:hidden sticky top-0 z-50 bg-white border-b border-stone-200">
    <div class="px-4 py-3 flex items-center justify-between">
      <%= link_to root_path do %>
        <%= image_tag "logo.png", alt: "ProfConnect", style: "height: 40px; width: auto;" %>
      <% end %>
      <% if user_signed_in? %>
        <div class="dropdown dropdown-end">
          <label tabindex="0" class="btn btn-ghost btn-circle avatar">
            <div class="w-9 rounded-full bg-gray-200 flex items-center justify-center overflow-hidden">
              <% if current_user.teacher? && current_user.teacher&.profile_image_url.present? %>
                <%= image_tag current_user.teacher.profile_image_url, alt: "Avatar", class: "w-full h-full object-cover" %>
              <% else %>
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                </svg>
              <% end %>
            </div>
          </label>
          <ul tabindex="0" class="menu menu-sm dropdown-content mt-3 z-[100] p-2 shadow bg-white rounded-box w-40">
            <li>
              <%= button_to "Déconnexion", destroy_user_session_path, method: :delete, class: "text-left w-full" %>
            </li>
          </ul>
        </div>
      <% end %>
    </div>
  </header>

  <!-- Desktop Header (hidden on mobile) -->
  <% if user_signed_in? %>
    <header class="hidden md:block sticky top-0 z-50 bg-white border-b border-stone-200">
      <div class="navbar max-w-7xl mx-auto px-4 md:px-6">
        <div class="flex-1">
          <%= link_to root_path, class: "flex items-center space-x-2" do %>
            <%= image_tag "logo.png", alt: "ProfConnect", style: "height: 50px; width: auto;" %>
          <% end %>
        </div>
        <div class="flex-none gap-6 items-center">
          <%= render "shared/navbar_logged_in" %>
        </div>
      </div>
    </header>
  <% else %>
    <header class="hidden md:block sticky top-0 z-50 bg-white border-b border-stone-200">
      <div class="navbar max-w-7xl mx-auto px-4 md:px-6">
        <div class="flex-1">
          <%= link_to root_path, class: "flex items-center space-x-2" do %>
            <%= image_tag "logo.png", alt: "ProfConnect", style: "height: 50px; width: auto;" %>
          <% end %>
        </div>
        <div class="flex-none flex items-center space-x-8">
          <%= link_to "Pourquoi ProfConnect ?", root_path(anchor: "pourquoi"), class: "text-sm font-medium text-gray-600 hover:text-gray-900" %>
          <%= link_to "FAQ", root_path(anchor: "faq"), class: "text-sm font-medium text-gray-600 hover:text-gray-900" %>
        </div>
      </div>
    </header>
  <% end %>

  <div class="max-w-2xl mx-auto px-4 md:px-6 py-12 md:py-16">
    <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-3">Nous contacter</h1>
    <p class="text-gray-700 leading-relaxed mb-8">
      Une question, un doute sur un envoi de document ou une suggestion ? Écrivez-nous et nous vous répondrons rapidement.
      Vous pouvez aussi nous joindre directement à
      <%= link_to "contact@prof-connect.fr", "mailto:contact@prof-connect.fr", class: "text-[#fe6618] hover:underline font-medium" %>.
    </p>

    <%= form_with model: @contact_message, url: contact_path, method: :post, class: "space-y-5" do |f| %>
      <% if @contact_message.errors.any? %>
        <div class="rounded-lg bg-red-50 border border-red-200 px-4 py-3 text-sm text-red-700">
          Merci de corriger les champs indiqués ci-dessous.
        </div>
      <% end %>

      <div>
        <%= f.label :name, "Nom", class: "block text-sm font-medium text-gray-900 mb-1" %>
        <%= f.text_field :name,
              class: "input input-bordered w-full bg-white #{'input-error' if @contact_message.errors[:name].any?}",
              autocomplete: "name" %>
        <% if @contact_message.errors[:name].any? %>
          <p class="mt-1 text-sm text-red-600"><%= @contact_message.errors[:name].to_sentence %></p>
        <% end %>
      </div>

      <div>
        <%= f.label :email, "Email", class: "block text-sm font-medium text-gray-900 mb-1" %>
        <%= f.email_field :email,
              class: "input input-bordered w-full bg-white #{'input-error' if @contact_message.errors[:email].any?}",
              autocomplete: "email" %>
        <% if @contact_message.errors[:email].any? %>
          <p class="mt-1 text-sm text-red-600"><%= @contact_message.errors[:email].to_sentence %></p>
        <% end %>
      </div>

      <div>
        <%= f.label :subject, "Sujet", class: "block text-sm font-medium text-gray-900 mb-1" %>
        <%= f.text_field :subject,
              class: "input input-bordered w-full bg-white #{'input-error' if @contact_message.errors[:subject].any?}" %>
        <% if @contact_message.errors[:subject].any? %>
          <p class="mt-1 text-sm text-red-600"><%= @contact_message.errors[:subject].to_sentence %></p>
        <% end %>
      </div>

      <div>
        <%= f.label :message, "Message", class: "block text-sm font-medium text-gray-900 mb-1" %>
        <%= f.text_area :message, rows: 6,
              class: "textarea textarea-bordered w-full bg-white #{'textarea-error' if @contact_message.errors[:message].any?}" %>
        <% if @contact_message.errors[:message].any? %>
          <p class="mt-1 text-sm text-red-600"><%= @contact_message.errors[:message].to_sentence %></p>
        <% end %>
      </div>

      <%# Honeypot anti-spam : doit rester vide, masqué aux utilisateurs humains %>
      <div class="hidden" aria-hidden="true">
        <%= f.label :company, "Ne pas remplir ce champ" %>
        <%= f.text_field :company, autocomplete: "off", tabindex: "-1" %>
      </div>

      <div class="pt-2">
        <%= f.submit "Envoyer le message",
              class: "btn border-0 bg-[#fe6618] hover:bg-[#d85514] text-white w-full md:w-auto px-8" %>
      </div>
    <% end %>
  </div>

  <!-- Footer -->
  <footer class="border-t border-stone-200">
    <div class="max-w-6xl mx-auto px-4 md:px-6 py-10">
      <div class="flex flex-col md:flex-row md:justify-between gap-8 md:gap-12">
        <div class="space-y-2">
          <div class="text-lg font-semibold text-[#fe6618]">ProfConnect</div>
          <p class="text-sm text-gray-500">© 2026 ProfConnect. Tous droits réservés.</p>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 flex-1 md:max-w-2xl">
          <!-- Colonne 1 : ProfConnect -->
          <div class="space-y-3">
            <h3 class="text-sm font-semibold text-gray-900 mb-3">ProfConnect</h3>
            <ul class="space-y-2 text-sm text-gray-600">
              <li>
                <%= link_to "Pourquoi ProfConnect ?", root_path(anchor: "pourquoi"), class: "hover:text-[#fe6618]" %>
              </li>
              <li>
                <%= link_to "FAQ", root_path(anchor: "faq"), class: "hover:text-[#fe6618]" %>
              </li>
              <li>
                <%= link_to "Nous contacter", contact_path, class: "hover:text-[#fe6618]" %>
              </li>
              <li>
                <a href="mailto:contact@prof-connect.fr" class="hover:text-[#fe6618]">contact@prof-connect.fr</a>
              </li>
            </ul>
          </div>
          <!-- Colonne 2 : Légal -->
          <div class="space-y-3">
            <h3 class="text-sm font-semibold text-gray-900 mb-3">Légal</h3>
            <ul class="space-y-2 text-sm text-gray-600">
              <li>
                <%= link_to "Conditions générales", conditions_generales_path, class: "hover:text-[#fe6618]" %>
              </li>
              <li>
                <%= link_to "Politique de confidentialité", politique_de_confidentialite_path, class: "hover:text-[#fe6618]" %>
              </li>
              <li>
                <%= link_to "Mentions légales", mentions_legales_path, class: "hover:text-[#fe6618]" %>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </footer>
</div>
